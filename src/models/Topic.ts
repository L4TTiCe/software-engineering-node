/**
 * @file Declares Topic data type representing a Topic in Tuiter
 */

/**
 * Represents a Topic
 * @typedef {Topic} Topic
 * @property {string} topic - The string that uniquely represents the Topic
 */
export interface Topic {
    topic: string;
}
