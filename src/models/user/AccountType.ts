/**
 * @file Declares AccountType enum representing the type of the {@link User}
 */

/**
 * An enum with different Account Types
 * @enum
 */
export enum AccountType {
    Personal = "PERSONAL",
    Academic = "ACADEMIC",
    Professional = "PROFESSIONAL",
}
