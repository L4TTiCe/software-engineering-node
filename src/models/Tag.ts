/**
 * @file Declares Tag data type representing a hashtag in Tuiter
 */

/**
 * Represents a hashtag
 * @typedef {Tag} Tag
 * @property {string} tag - The string that uniquely represents the Tag
 */
export interface Tag {
    tag: string;
}
